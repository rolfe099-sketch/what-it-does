// A browser-only app. There is no server here, which is the point of the
// fixture: the tool must say so as a finished answer rather than promising a
// future extractor.
export default function App() {
  return <main>Hello</main>;
}
