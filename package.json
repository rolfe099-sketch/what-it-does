{
  "name": "what-it-does",
  "version": "0.3.1",
  "description": "Shows you what software you didn't write actually does, and tells you when that changes. Runs locally; your code never leaves the machine.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rolfe099-sketch/what-it-does.git"
  },
  "homepage": "https://github.com/rolfe099-sketch/what-it-does#readme",
  "bugs": {
    "url": "https://github.com/rolfe099-sketch/what-it-does/issues"
  },
  "bin": {
    "what-it-does": "dist/cli.js"
  },
  "files": [
    "dist",
    "design/fonts",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "static-analysis",
    "nextjs",
    "codebase",
    "documentation",
    "dependency-graph"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsc",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "tsc --noEmit && tsc -p tsconfig.test.json && npm test",
    "scan": "tsx src/cli.ts scan",
    "demo": "bash make-demo.sh",
    "prepack": "npm run build",
    "prepublishOnly": "npm run check"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6"
  }
}
